use crate::lexer::Token;

grammar;

pub Thing: String = {
	<"num"> "eol"? => <>,
};

extern {
	type Location = usize;
	type Error = ();

	enum Token {
		"num" => Token::LitNumeric(<String>),
		"eol" => Token::EOL,
	}
}