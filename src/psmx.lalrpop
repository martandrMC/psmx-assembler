use crate::lexer::Token;

grammar;

pub Thing: (usize, String, usize) = {
	<@L> <"num"> <@R> "eol"? => (<>),
};

extern {
	type Location = usize;
	type Error = ();

	enum Token {
		"num" => Token::LitNumeric(<String>),
		"eol" => Token::EOL,
	}
}